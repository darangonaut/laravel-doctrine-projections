<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <!-- Generation and SQL classification are pure transformations. -->
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!-- The lock can only be verified by actually attempting writes. -->
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <!-- Commands through a real Laravel application (testbench). -->
        <testsuite name="feature">
            <directory>tests/Feature</directory>
        </testsuite>
        <!--
            Doctrine and the projections on one database, answering the
            same questions. Asserts they agree rather than asserting a
            specific answer, so a mapping shape nobody wrote a test for
            is still covered.
        -->
        <testsuite name="differential">
            <directory>tests/Differential</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
